{
  "name": "lint-staged-demo",
  "version": "1.0.0",
  "description": "Demo autofix issue",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "lint-staged",
    "lint": "eslint *.js"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "git add"]
  },
  "author": "Samuel Sharpe",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.19.0",
    "husky": "^0.13.3",
    "lint-staged": "^3.4.0"
  }
}
